{
  "name": "dubizzle-lk-stats",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1 && npm run sentry:sourcemaps",
    "sentry:sourcemaps": "sentry-cli sourcemaps inject --org globaldrive --project node ./out && sentry-cli sourcemaps upload --org globaldrive --project node ./out"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@sentry/cli": "^2.25.0",
    "@sentry/node": "^7.93.0",
    "@sentry/profiling-node": "^1.3.5",
    "firebase-admin": "^12.0.0",
    "fs": "^0.0.1-security",
    "google-auth-library": "^9.4.2",
    "google-spreadsheet": "^4.1.1",
    "puppeteer": "^21.7.0"
  }
}